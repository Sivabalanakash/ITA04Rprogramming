# Create an empty plot without specifying axis limits
plot(NA, xlim = c(0, 10), ylim = c(0, 20), xlab = "X Axis", ylab = "Y Axis", main = "Empty Plot")

# Create a new empty plot with specified axis limits
plot(NA, xlim = c(0, 5), ylim = c(0, 10), xlab = "X Axis", ylab = "Y Axis", main = "Empty Plot with Axis Limits")
